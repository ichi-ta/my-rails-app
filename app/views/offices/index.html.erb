<% provide(:title, '事業所一覧') %>
<div>
<%= link_to 'back', blogs_path%>
</div>

<h1>事業所の一覧</h1>
<ul class="offices">
  <% @offices.each do |office| %>
    <li>
      <%= link_to office.name, office %>
    </li>
  <% end %>
</ul>

